package Ex_05_06_07_Perguntas_TAD_Pilha;

public class Respostas {
	
	/*
	 * 5) Suponha que uma pilha inicialmente vazia S tenha executado um total de 25 operações
	 * push, 12 operações top e 10 operações pop, 3 das quais geraram StackEmptyExceptions, que 
	 * foram capturadas e ignoradas. Qual é o tamanho corrente de S?
	 * 
	 * Reposta: O tamanho corrente de S é 18.
	 * 
	 * 
	 * 
	 * 6) Se implementarmos a pilha S do problema anterior usando um arranjo, então qual
	 * será o valor corrente da variável de instância top?
	 * 
	 * Reposta: O tamanho corrente de S é 17.
	 * 
	 * 
	 * 
	 * 7) Descreva a saída resultante da seguinte série de operações de pilha: push(5), push(3),
	 * pop( ),push(2), push(8), pop( ), pop( ), push(9), push(1), pop( ), push(7), push(6), pop(),
	 * pop(), push(4), pop(), pop( ).
	 * 
	 * Reposta: A saida será o número 5.
	 */

}
